# Brave datasets

Unmodified copies of `clean-urls.json`
and `debounce.json` from [brave/adblock-lists](https://github.com/brave/adblock-lists), licensed under the MPL-2.0.
`UPSTREAM` records the commit they were taken from.
Refresh them with `mise run rules-update`; never edit them by hand.
