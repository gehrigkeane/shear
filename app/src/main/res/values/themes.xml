<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the UI; the platform theme only needs to stay out of the way. -->
    <style name="Theme.Shear" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- For the relay activity: transparent so the offline path draws nothing, yet able to host the progress overlay. -->
    <style name="Theme.Shear.Relay" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
